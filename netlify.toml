[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NETLIFY_SKIP_SECRETS_CHECK = "true"

[functions]
  external_node_modules = []
  node_bundler = "esbuild"

# Claude API proxy now runs as edge function for better streaming support

[functions."perplexity"]
  timeout = 30
