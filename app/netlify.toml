[build]
  publish = "."
  functions = "netlify/functions"

# App at /app (with and without trailing slash)
[[redirects]]
  from = "/app"
  to = "/index.html"
  status = 200

[[redirects]]
  from = "/app/"
  to = "/index.html"
  status = 200
